events {}

http {
    server {
        listen 80;
        server_name sgc-agmeal.com;

        location / {
            proxy_pass http://agmeal-app:5000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}